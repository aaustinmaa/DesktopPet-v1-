<Window x:Class="DesktopPet.FocusExportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="导出 Markdown"
        Width="450" Height="440"
        MinWidth="420" MinHeight="420"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="{StaticResource SurfaceBrush}"
        FontFamily="Microsoft YaHei UI"
        Foreground="{StaticResource InkBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="导出专注记录"
                       FontSize="24"
                       FontWeight="Bold"/>
            <TextBlock Text="选择日期范围后，将记录整理成一个 Markdown 文件。"
                       Margin="0,5,0,0"
                       Foreground="{StaticResource MutedBrush}"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <Border Grid.Row="1"
                Style="{StaticResource ModernCardStyle}"
                Margin="0,18,0,18">
            <StackPanel>
                <TextBlock Text="开始日期" FontWeight="SemiBold"/>
                <DatePicker x:Name="StartDatePicker"
                            Margin="0,6,0,14"/>
                <TextBlock Text="结束日期" FontWeight="SemiBold"/>
                <DatePicker x:Name="EndDatePicker"
                            Margin="0,6,0,0"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="取消"
                    Width="86"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Click="Cancel_Click"/>
            <Button Content="选择位置并导出"
                    MinWidth="128"
                    Click="Export_Click"/>
        </StackPanel>
    </Grid>
</Window>
