# 苏无度项目 Agent 工作规则

## 当前工作区是唯一事实来源

这个项目会同时由多个 Codex / ChatGPT thread 并行处理。任何一次任务开始前，仓库内容都可能已经被其它 thread、agent 或用户修改。

- 不要依赖之前对话、之前 thread、上一次 turn 或模型记忆中的代码状态。
- 不要假设自己以前看过的文件仍然保持原样。
- 只把当前工作区中实际存在的文件和当前用户请求视为可靠信息。
- 在修改目标文件前，重新读取与当前任务直接相关的最新内容。
- 已有修改可能属于用户或其它 thread；不要擅自撤销、覆盖、整理或“恢复”它们。
- 不要因为代码与记忆中不同就判断它是错误的；这通常只是其它 thread 已经更新了项目。

## 严格限制当前任务范围

- 只修改用户这一次明确要求修改的内容。
- 不要顺手修复、重构、格式化或清理当前请求之外的代码。
- 不要根据上一次任务自行延续工作。
- 如果当前请求只涉及开发版，不要修改、重建、验证或检查安装包。
- 只有用户明确要求更新安装包时，才可以运行 `build-installer.ps1`，或改动 `Installer/` 与 `dist/苏无度安装程序.exe`。
- `build.ps1` 用于开发版；不要把运行它自动扩大为安装包发布工作。

## 不检查上一次改动

- 不要为了确认、补救或完善自己上一次的改动而进行 testing、checking、review 或额外修改。
- 不要重新运行上一次任务的测试、构建、安装流程或 UI 验证。
- 不要审查其它 thread 已完成的工作，除非用户在当前请求中明确要求。
- 验证只能针对当前请求中新产生的改动，并且范围必须与当前请求相称。
- 如果当前请求只是写文档、回答问题或修改单个资源，不要借机测试整个应用。

## 开发版与安装包

- 开发版入口：`dist/SuWuDu/SuWuDu.exe`
- 开发版构建：`build.ps1`
- 安装包入口：`dist/苏无度安装程序.exe`
- 安装包构建：`build-installer.ps1`

两者必须视为不同的交付范围。修改开发版不代表获得了修改安装包的授权；修改安装包也必须由用户明确提出。

